# Evitar a criação de .package-lock.json em ambientes CI
save-exact=true
# Ignorar scripts durante instalação no CI
scripts-prepend-node-path=true

# Usar versões exatas para maior reprodutibilidade
save-exact=true

# Configuração para ambientes CI
if-present=true

# Ignora alertas de vulnerabilidade durante o CI
audit=false
